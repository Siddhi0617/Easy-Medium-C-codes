#include <stdio.h>
int main()
 {
    int rotations;
    printf("Enter number of wheel rotations: ");
    scanf("%d", &rotations);
    printf("Robot moved %d rotations forward.\n", rotations);
    return 0;
}
